<h2>OrgFile Locations</h2>
<ul>
    <%= for orgfile <- @orgfiles do %>
        <li><strong><%= orgfile.filename %></strong> @
            <%= orgfile.host %> on path: <%= orgfile.path %>></li>
    <% end %>
</ul>

<a href="<%= Routes.page_path(@conn, :index) %>">HOME</a>
